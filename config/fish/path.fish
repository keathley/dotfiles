set GOPATH $HOME/go

set PATH $PATH \
         ./bin \
         ~/.bin \
         ~/bin \
         /usr/local/bin \
         /usr/local/sbin \
         ./node_modules/.bin \
         ~/.cargo/bin \
         $GOPATH \
         $GOPATH/bin

